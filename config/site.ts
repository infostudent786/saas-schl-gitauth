import siteData from './site.json';

/**
 * siteConfig is imported directly from site.json.
 * This ensures that when Decap CMS updates the JSON file, 
 * the changes are immediately reflected in the app.
 */
const siteConfig = siteData;

export default siteConfig;